This is a Code::Blocks project to compile CudaGol

It's a normal g++ (or whatever) project, only the .cu file has a different compile setting.

nvcc needs to be in the systems search path to make this work